OPENAI_API_KEY=sk-your-key-here
# Optional: comma-separated origins for browser chat (Vercel URL, localhost). Empty = allow all (demo only).
# ALLOWED_ORIGINS=https://your-site.vercel.app,http://127.0.0.1:5500,http://localhost:4173
