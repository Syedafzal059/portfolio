OPENAI_API_KEY=sk-your-key-here
# Optional: comma-separated extra origins (e.g. custom domain). All https://*.vercel.app and localhost are also allowed.
# Empty or * = allow any origin (demo only). Example:
# ALLOWED_ORIGINS=https://your-custom-domain.com
